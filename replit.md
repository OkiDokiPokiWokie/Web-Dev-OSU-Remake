# Web Dev Class OSU Remake

A blank PHP project for web development class.

## Run & Operate

- Click the green Run button to start the PHP built-in server
- Server runs on port 8080, serving `index.php` from the project root

## Stack

- PHP (built-in web server)

## User preferences

- Keep the project minimal — no extra files, no frameworks, just PHP.
